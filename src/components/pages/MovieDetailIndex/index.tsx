import React from 'react';

export type MovieDetailIndexProps = {};

const MovieDetailIndex = ({}: MovieDetailIndexProps): React.ReactElement => {
    return <h1>MOVIEW DETAIL</h1>;
};

export default MovieDetailIndex;
